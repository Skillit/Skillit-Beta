<% @user.each do |user| %>
	<p>
		<%= link_to 'Name', user_path(user) %>: <%= user.first_name %> <%= user.last_name %><br>
		About: <%= user.about %> <br>
		Email: <%= user.email %> <br>

		

	</p>

<% end %>

